DB_HOST=localhost
DB_USER=technicien
DB_PASSWORD=change-me
DB_NAME=samv2
DB_PORT=3306

DEFAULT_RTSP_USER=admin
DEFAULT_RTSP_PASS=change-me

SECRET_KEY=change-this-random-secret
API_BASE_URL=http://127.0.0.1:5000
FLASK_DEBUG=False

# Set to True when the app is served over HTTPS.
SESSION_COOKIE_SECURE=False

# Set TRUST_PROXY_HEADERS=True behind a trusted reverse proxy that sets X-Forwarded-Proto.
# Set ENABLE_HSTS=True only when HTTPS is enforced for the public host.
TRUST_PROXY_HEADERS=False
ENABLE_HSTS=False
HSTS_MAX_AGE=31536000
